import React from 'react';

// Placeholder until we wire subscription list endpoint

const SubscriptionTable: React.FC = () => {
  return (
    <div className="border border-slate-800 rounded-lg p-4 text-sm text-slate-300">
      Subscription table placeholder. We will hook this to a subscriptions endpoint later.
    </div>
  );
};

export default SubscriptionTable;
